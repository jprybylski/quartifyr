[project]
name = "memo-example"
version = "0.0.1"
requires-python = ">=3.12"

[dependency-groups]
reportifyr = [
    "python-docx==1.1.2",
    "pyyaml==6.0.2",
    "pillow==11.1.0",
]
quartifyr = [
    "python-docx",
    "pyyaml",
]

