% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/initialize.R
\name{.ensure_default_groups_all}
\alias{.ensure_default_groups_all}
\title{Ensure a project's pyproject.toml defaults every dependency group on}
\usage{
.ensure_default_groups_all(pyproject_path)
}
\arguments{
\item{pyproject_path}{Path to the \code{pyproject.toml} to check/edit.}
}
\value{
\code{TRUE} if the file was modified, \code{FALSE} otherwise (invisibly).
}
\description{
A report project's \code{pyproject.toml} ends up with more than one
\verb{[dependency-groups]} entry sharing one venv: \code{reportifyr}'s own
(from \code{reportifyr::initialize_report_project()}) and \code{quartifyr}'s
(above). \verb{uv run}/\verb{uv sync} only include \emph{default} groups unless a
call explicitly passes \code{--group}/\code{--all-groups} -- and both
\code{reportifyr::validate_docx()} (external, \code{reportipyr.cli}) and, before
a fix, this package's own \code{.run_quartifyr_styling_cli()} invoke a bare
\verb{uv run} with no group flag at all, which resyncs the venv down to
\emph{zero} groups, silently uninstalling whichever group(s) aren't
explicitly requested -- confirmed the hard way against a fully fresh
venv in CI, as an import error inside whichever tool ran second.
\code{default-groups = "all"} (uv's own supported \verb{[tool.uv]} config, not a
quartifyr invention) makes \emph{every} bare \verb{uv run}/\verb{uv sync} call include
every declared group by default, which is the only fix available for
the bug in \code{reportifyr::validate_docx()} specifically, since that's
external code this package doesn't control.
}
\keyword{internal}
