% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run-python.R
\name{.run_quartifyr_styling_cli}
\alias{.run_quartifyr_styling_cli}
\title{Invoke the bundled quartifyr-styling Python CLI via pyro}
\usage{
.run_quartifyr_styling_cli(args)
}
\arguments{
\item{args}{Character vector of CLI arguments \emph{after} \code{quartifyr-styling},
e.g. \code{c("build", "--style", "path/to/style.yaml", "--out", "out.docx")}.}
}
\value{
A parsed list from the CLI's JSON output.
}
\description{
Internal helper every exported \verb{styling_*()} function funnels through --
the single bridge point, so docx-generation/layout logic is never
reimplemented in R (mirrors ../deckifyr's \code{.run_deckifyr_cli()} pattern).
Always requests \code{--json} so results come back as a parsed R list rather
than needing prose scraping.
}
\details{
\strong{Why this isn't a thin wrapper around \code{pyro::run_python_script()}:}
confirmed against a real pyro install, \code{run_python_script()} calls
\code{processx::run(..., error_on_status = TRUE)} and, on any non-zero exit,
discards the captured stdout/stderr entirely and raises a bare
"<script_name> failed." -- so \code{quartifyr-styling}'s own diagnostic JSON
(emitted to stderr on error when \code{--json} is passed, see
\code{inst/python/quartifyr_styling/cli.py}'s module docstring) would
otherwise be lost. The fix is \code{stderr_callback}: processx invokes it per
output chunk \emph{while the process is still running}, before the exit-status
check fires, so output captured that way survives even when the call
ultimately errors. This only works because \code{cli.py} deliberately writes
its JSON error payload to stderr rather than stdout on the error path --
don't change one side of this handshake without the other.
}
\keyword{internal}
