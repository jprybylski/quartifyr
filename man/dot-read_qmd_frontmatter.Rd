% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate-header.R
\name{.read_qmd_frontmatter}
\alias{.read_qmd_frontmatter}
\title{Parse a .qmd's raw YAML frontmatter block}
\usage{
.read_qmd_frontmatter(shell_qmd)
}
\arguments{
\item{shell_qmd}{Path to the \code{.qmd} file.}
}
\value{
The parsed frontmatter as a list (\code{list()} if the file has none).
}
\description{
Reads the block between the first two \verb{---} lines directly with \code{yaml},
the same way \code{inst/python/quartifyr_styling/layout.py}'s
\code{read_qmd_frontmatter()} does -- deliberately \emph{not} \code{quarto::quarto_inspect()},
which resolves frontmatter through Quarto's own config engine and, for
this extension's purposes, actively gets one thing wrong: \verb{project:} is
a reserved Quarto project-config key, and Quarto's inspector silently
drops a document-level \verb{project:} field from its parsed metadata (e.g.
\code{examples/demo-report/report.qmd}'s \code{project: "ACME-001"}, fed into
\code{header-format: "{project} - {report_number}"}). \code{apply-layout}'s own
\verb{header-format:} resolution reads raw frontmatter for exactly this
reason, so \code{validate_header()} has to match it to check the same thing
it actually affects. This also means \code{validate_header()} needs no
Quarto installation at all -- useful for a brand-new project that
hasn't set one up yet (see \code{header_helper()}).
}
\keyword{internal}
