% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/skills.R
\name{styling_export_skills}
\alias{styling_export_skills}
\title{Export quartifyr's bundled coding-agent skill files}
\usage{
styling_export_skills(directory = ".", force = FALSE)
}
\arguments{
\item{directory}{Target directory. Defaults to the current directory.}

\item{force}{Overwrite an existing \code{SKILL.md} at the destination.
Default \code{FALSE}.}
}
\value{
A list describing the created files (invisibly).
}
\description{
Copies the package's bundled coding-agent skill files (Claude
Skills-format \code{SKILL.md}s) into \code{directory}: one skill for authoring a
style YAML (org-level typography/page/table configuration), and one for
authoring a shell \code{.qmd} (front matter, appendices, captions, magic-string
placeholders). Each lands under its own \verb{<directory>/<skill-name>/SKILL.md}
subdirectory, since a \code{SKILL.md} file's name is fixed by the Skills
convention -- point \code{directory} at \code{.claude/skills} for Claude Code to
auto-discover them, or anywhere else for a different coding agent/tool, or
just to inspect the content.
}
\examples{
\dontrun{
styling_export_skills(".claude/skills")
}
}
