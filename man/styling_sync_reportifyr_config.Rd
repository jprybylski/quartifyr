% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sync-reportifyr-config.R
\name{styling_sync_reportifyr_config}
\alias{styling_sync_reportifyr_config}
\title{Sync reportifyr's config.yaml footnote styling to a quartifyr style YAML}
\usage{
styling_sync_reportifyr_config(
  style,
  override = NULL,
  config_yaml = "report/config.yaml",
  yes = FALSE
)
}
\arguments{
\item{style}{Path to the base style YAML.}

\item{override}{Optional per-org/per-project style YAML, deep-merged over
\code{style}. \code{NULL} (default) to skip.}

\item{config_yaml}{Path to reportifyr's \code{config.yaml}, modified in place.}

\item{yes}{Skip the confirmation requirement and write directly.}
}
\value{
\code{TRUE} if \code{config_yaml} was changed, \code{FALSE} if it was already in
sync (invisibly).
}
\description{
reportifyr doesn't read quartifyr's style YAML at all -- its own
\code{report/config.yaml} scaffolds \code{footnotes_font}/\code{footnotes_font_size}
independently (defaulting to "Arial Narrow"/10), so left untouched they
visually clash with whatever \code{fonts.body}/\code{fonts.sizes.footnote} a style
YAML sets for the rest of the document. This updates just those two
keys to match.
}
\details{
Requires an interactive \code{y}/\code{yes} confirmation before writing unless
\code{yes = TRUE} is passed -- there's no live stdin to confirm over via this
R wrapper (it always calls the underlying CLI with \code{--json}, a
non-interactive/piped invocation), so \code{yes = TRUE} is required whenever
\code{config_yaml}'s footnote keys are actually out of sync; a call that
finds nothing to change succeeds either way.
}
\examples{
\dontrun{
# Requires a provisioned quartifyr Python environment and an existing
# reportifyr report/config.yaml (see reportifyr::initialize_report_project()).
styling_sync_reportifyr_config("style.yaml", config_yaml = "report/config.yaml", yes = TRUE)
}
}
