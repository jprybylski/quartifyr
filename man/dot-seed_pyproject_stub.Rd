% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/initialize.R
\name{.seed_pyproject_stub}
\alias{.seed_pyproject_stub}
\title{Pre-seed a minimal pyproject.toml before pyro can seed it incorrectly}
\usage{
.seed_pyproject_stub(directory)
}
\arguments{
\item{directory}{Target project directory (already normalized).}
}
\value{
\code{TRUE} if the file was created, \code{FALSE} if it already existed
(invisibly).
}
\description{
\code{pyro::write_group_to_pyproject()} is a no-op when \code{pyproject.toml}
doesn't exist yet (it only edits an existing file). On a project where
\code{reportifyr::initialize_report_project()} hasn't already created that
file, that leaves \code{pyro::initialize_python(groups = "quartifyr")} --
called right after -- to seed it instead, via \code{pyro}'s own
\code{seed_pyproject()}. \code{"quartifyr"} isn't one of the group names in
\code{pyro}'s \emph{bundled} spec (only \code{reportifyr}/\code{presentifyr} are), and
\code{pyro}'s seeding falls back to \code{character()} deps for unrecognized
groups -- which its \code{render_subgroup()} then renders as a single
spurious empty-string entry (\verb{quartifyr = [\\n    "",\\n]}) due to an
R \code{paste0()} recycling quirk (\code{paste0("x", character(0))} returns
\code{"x"}, not \code{character(0)}). \code{uv} rejects that as "Empty field is not
allowed for PEP508", breaking \code{initialize_quartifyr_project()} on a
project where it's the first \code{pyro}-based initializer to run.
Confirmed directly against the installed \code{pyro} package (0.1.1); see
\code{tests/testthat/test-initialize.R}.
}
\details{
Writing a minimal, valid \code{pyproject.toml} here -- before
\code{write_group_to_pyproject()}/\code{initialize_python()} run -- ensures the
file already exists by the time \code{pyro::initialize_python()} checks, so
it skips its own (buggy, for unrecognized group names) seeding step
entirely; \code{write_group_to_pyproject()} is what ends up creating the
\code{quartifyr} group instead, with the real, non-empty deps this package
controls. \code{requires-python = ">=3.12"} matches \code{pyro}'s own bundled
spec (not this repo's separate, looser \verb{>=3.10} for the standalone
\code{quartifyr-styling} Python package) so the constraint is the same
regardless of whether this or \code{reportifyr::initialize_report_project()}
seeds the shared venv's \code{pyproject.toml} first.
}
\keyword{internal}
