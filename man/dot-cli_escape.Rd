% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate-header.R
\name{.cli_escape}
\alias{.cli_escape}
\title{Escape literal \verb{\{}/\verb{\}} for cli's glue-style interpolation}
\usage{
.cli_escape(x)
}
\description{
Field descriptions and structural-check messages are plain prose that
quotes YAML syntax (\verb{\{label, value\}}, \verb{header-format:}'s own
\code{{placeholder}} templates, ...) -- cli's \verb{\{.field ...\}}-style markup
uses the same glue delimiters, so any of that prose reaching
\code{cli_bullets()}/\code{cli_text()} unescaped is misparsed as an expression to
evaluate (confirmed: \verb{\{name, title\}} in the \code{contributors} field's own
description raised a glue parse error). Doubling every brace in
free-form text before splicing it next to real \code{{.field}}/\code{{.strong}}
markup (added separately, already valid) is the standard glue escape.
}
\keyword{internal}
