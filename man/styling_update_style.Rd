% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update-style.R
\name{styling_update_style}
\alias{styling_update_style}
\title{Deep-merge an update onto an existing style YAML, in place}
\usage{
styling_update_style(updates, file, yes = FALSE)
}
\arguments{
\item{updates}{A (possibly nested) list of the values to change, e.g.
\code{list(heading = list(all_caps = TRUE))}.}

\item{file}{The style YAML to update, in place.}

\item{yes}{Must be \code{TRUE} to actually write -- see above.}
}
\value{
The style YAML's full merged content, as a nested list
(invisibly).
}
\description{
A \code{modifyList()}-style edit of one section of a style YAML, without
hand-copying the rest of the file.
}
\details{
Requires \code{yes = TRUE}: there's no live stdin to confirm over via this R
wrapper (it always calls the underlying CLI with \code{--json}, a
non-interactive/piped invocation, same as \code{\link[=styling_sync_reportifyr_config]{styling_sync_reportifyr_config()}}),
and this always has \emph{something} to change (unlike that function, which
can no-op) -- passing \code{yes = TRUE} is this function's explicit
acknowledgement that rewriting the file via \code{yaml.safe_dump} drops any
comments/formatting it had.
}
\examples{
\dontrun{
# Requires a provisioned quartifyr Python environment (see
# initialize_quartifyr_project()).
styling_update_style(list(heading = list(all_caps = TRUE)), "style.yaml", yes = TRUE)
}
}
