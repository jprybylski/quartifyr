% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/example-style.R
\name{styling_example_style}
\alias{styling_example_style}
\title{Copy the bundled example style YAML into a project}
\usage{
styling_example_style(
  dir = ".",
  file = "style.yaml",
  overwrite = FALSE,
  base = system.file("python", "styles", "default.yaml", package = "quartifyr")
)
}
\arguments{
\item{dir}{Destination directory. Created if it doesn't exist.}

\item{file}{Destination filename within \code{dir}.}

\item{overwrite}{Replace the destination if it already exists.}

\item{base}{Style YAML to copy. Defaults to the package's own bundled
\code{default.yaml}; only worth overriding to start from an existing org
style YAML instead.}
}
\value{
The copied style YAML, parsed into a nested list.
}
\description{
A convenience over \code{file.copy(system.file("python", "styles", "default.yaml", package = "quartifyr"), ...)} (issue #27) that also
returns the copy's parsed content as an R list, ready to edit in place
and hand to \code{\link[=styling_save_overrides]{styling_save_overrides()}}.
}
