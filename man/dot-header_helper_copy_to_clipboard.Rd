% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/header-helper.R
\name{.header_helper_copy_to_clipboard}
\alias{.header_helper_copy_to_clipboard}
\title{Try to copy text to the clipboard via the optional \code{clipr} package}
\usage{
.header_helper_copy_to_clipboard(text)
}
\arguments{
\item{text}{The text to copy.}
}
\value{
\code{TRUE} if copied, \code{FALSE} if \code{clipr} isn't installed, has no
clipboard available, or the copy itself failed.
}
\description{
Isolated in its own function (rather than inline in \code{header_helper()})
so tests can mock this one call instead of reaching into \code{clipr}'s own
namespace, which \code{testthat::local_mocked_bindings()} advises against
for functions called with \code{::} (see \code{?testthat::local_mocked_bindings}'s
"Namespaced calls" section) -- and so a real run doesn't depend on the
test environment actually having clipboard access.
}
\keyword{internal}
