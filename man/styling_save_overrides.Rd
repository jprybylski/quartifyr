% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save-overrides.R
\name{styling_save_overrides}
\alias{styling_save_overrides}
\title{Save a (possibly edited) style list as an override YAML}
\usage{
styling_save_overrides(
  style,
  file = "overrides.yaml",
  overwrite = FALSE,
  deconvolute = TRUE,
  base = system.file("python", "styles", "default.yaml", package = "quartifyr")
)
}
\arguments{
\item{style}{The (possibly edited) style list, typically
\code{\link[=styling_example_style]{styling_example_style()}}'s return value after in-place edits.}

\item{file}{Destination path for the override YAML.}

\item{overwrite}{Replace the destination if it already exists.}

\item{deconvolute}{Save only the keys that differ from \code{base} (the
default) -- an override meant to be deep-merged back onto that same
base at load time, not a second full style YAML to keep in sync by
hand. \code{FALSE} saves \code{style} in full instead.}

\item{base}{Style YAML \code{style} is diffed against when \code{deconvolute = TRUE}. Defaults to the package's own bundled \code{default.yaml}; only
worth overriding if \code{style} didn't originate from
\code{\link[=styling_example_style]{styling_example_style()}}'s default \code{base} either.}
}
\value{
The saved content, as a nested list (invisibly).
}
\description{
Complements \code{\link[=styling_example_style]{styling_example_style()}} (issue #27): edit its returned
list in place, then save just what changed back out as an override
YAML, ready to pass as \code{override}/\code{--override} to
\code{\link[=styling_build_reference_docx]{styling_build_reference_docx()}} or any other \verb{styling_*()} function
that takes one.
}
