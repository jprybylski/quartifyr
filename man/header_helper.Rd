% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/header-helper.R
\name{header_helper}
\alias{header_helper}
\title{Interactively build a quartifyr front-matter block}
\usage{
header_helper(doc_type = c("report", "memo"), clipboard = TRUE)
}
\arguments{
\item{doc_type}{\code{"report"} (title page) or \code{"memo"} (memo cover).}

\item{clipboard}{Try to copy the result to the clipboard. Default \code{TRUE}.}
}
\value{
Invisibly, the generated YAML block (a single string, including
the \verb{---} fences).
}
\description{
Walks the same field registry \code{validate_header()} validates against
(see \code{R/header-fields.R}), prompting for required fields, offering
recommended/optional ones, and looping through the list-shaped fields
(\verb{title-page-extra:}, \verb{address:}, \verb{synopsis:}, \verb{contributors:},
\verb{approvers:}) a row/person at a time. The result is a ready-to-paste
YAML front-matter block -- printed to the console and, when available,
copied to the clipboard (via the optional \code{clipr} package), the same
"copy or print" convenience \code{reprex::reprex()} offers for its output.
}
\details{
Evergreen by the same construction as \code{validate_header()}: a field
added to the registry is automatically prompted for here too, with no
separate wording to maintain.
}
\section{Example session}{

\code{header_helper()} requires a real interactive session (it aborts
otherwise -- see \verb{@examples} below), so this can't be executed on the
pkgdown/\verb{R CMD check} example run the way \code{validate_header()}'s can.
The transcript below is a real captured run of the underlying prompt
loop, reformatted the way it reads in a terminal (each \verb{[y/N]}/\verb{[Y/n]}
question abbreviated to just the ones answered "yes", since most of
several dozen optional fields are skipped in any one run):

\if{html}{\out{<div class="sourceCode">}}\preformatted{> header_helper("report")
── quartifyr header helper (report) ──

Title (required): Population PK Analysis of Compound X
Set Subtitle? -- Optional subtitle line under the title. [y/N] n
Set Report type? -- ... [y/N] n
Set Date? -- Info-table row. ... [y/N] y
Date: 2026-08-19
Set Lead scientist? -- Info-table row. [y/N] n
...
── Title page ──

Add an address line? [y/N] y
  Line: Acme Pharma
Add one more address line? [y/N] n
Add an extra title-page row? [y/N] n
── Signature page ──

Add an author? [y/N] y
  Name: Jane Doe, PharmD
  Title: Lead Scientist
Add one more author? [y/N] n
Add a reviewer? [y/N] n
Add an approver? [y/N] n
── Synopsis ──

Add a synopsis? [y/N] n

── Generated front matter ──

---
title: Population PK Analysis of Compound X
date: '2026-08-19'
address:
  - Acme Pharma
contributors:
  authors:
    - name: Jane Doe, PharmD
      title: Lead Scientist
format: docx
filters:
  - quarto-plus
  - quartifyr
---

Copied to the clipboard -- paste it at the top of your .qmd.
}\if{html}{\out{</div>}}
}

\examples{
\dontrun{
# Interactive only -- run this at the console, not via source()/Rscript.
header_helper("report")
header_helper("memo")
}
}
