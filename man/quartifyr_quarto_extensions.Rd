% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quarto-extensions.R
\name{quartifyr_quarto_extensions}
\alias{quartifyr_quarto_extensions}
\title{Quarto extensions quartifyr's own output relies on}
\format{
A data frame with columns:
\describe{
\item{id}{Extension identifier, in the form \code{quarto::quarto_add_extension()}/\verb{quarto add} accepts (an \code{owner/repo} GitHub spec).}
\item{required}{\code{TRUE} if a render is expected to fail (or silently misbehave) without it; \code{FALSE} if it's merely recommended.}
\item{reason}{One-line explanation of what quartifyr uses it for, shown in \code{\link[=check_quarto_extensions]{check_quarto_extensions()}}'s diagnostic messages.}
}
}
\usage{
quartifyr_quarto_extensions
}
\description{
A small registry of third-party Quarto extensions that quartifyr's
bundled \verb{inst/extensions/quartifyr/} templates and \code{render_report()}'s
output assume are present in a project's \verb{_extensions/}, checked by
\code{\link[=check_quarto_extensions]{check_quarto_extensions()}}. Quarto's own \verb{_extension.yml} has no
"requires this other extension" field, so this list is quartifyr's own
substitute -- kept as data rather than hardcoded into the check
function so a future additional dependency (required or merely
suggested) is a one-line addition here, not a rewrite of the check
itself. Doesn't include quartifyr's own extension: that one is
installed and verified by \code{\link[=install_quartifyr_extension]{install_quartifyr_extension()}} directly,
not \code{quarto::quarto_add_extension()}.
}
\examples{
quartifyr_quarto_extensions
}
