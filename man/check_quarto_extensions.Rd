% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quarto-extensions.R
\name{check_quarto_extensions}
\alias{check_quarto_extensions}
\title{Check that quartifyr's required/suggested Quarto extensions are installed}
\usage{
check_quarto_extensions(
  path = ".",
  extensions = quartifyr_quarto_extensions,
  error = TRUE
)
}
\arguments{
\item{path}{Project directory to check (the one containing, or that
should contain, \verb{_extensions/}). Defaults to the current directory.}

\item{extensions}{Registry to check against. Defaults to
\link{quartifyr_quarto_extensions}; pass your own data frame in the same
shape (optionally \code{rbind()}'d onto the default) to also check
project-specific extensions.}

\item{error}{Whether a missing extension with \code{required = TRUE} raises
an error (default \code{TRUE}, and this is what \code{render_report()} uses).
Extensions with \code{required = FALSE} never error, only warn, regardless
of this argument.}
}
\value{
\code{extensions} with an added logical \code{installed} column
(invisibly).
}
\description{
Compares \link{quartifyr_quarto_extensions} (or a caller-supplied data frame
in the same shape) against \code{quarto::quarto_list_extensions()}'s output
for \code{path}, and reports anything missing with a clickable
\code{quarto::quarto_add_extension()} suggestion for each one.
\code{render_report()} calls this itself before rendering, so a missing
\emph{required} extension stops the render with an actionable message
instead of Quarto's own much less specific "could not find executable"
filter failure. Call it directly to check a project on its own, e.g.
right after \code{\link[=install_quartifyr_extension]{install_quartifyr_extension()}}.
}
\section{A \code{quarto_list_extensions()} parsing gotcha}{

\code{quarto::quarto_list_extensions()} feeds the \verb{quarto list extensions}
CLI's plain-text table straight into \code{read.table(text = ..., header = TRUE, fill = TRUE, sep = "")} (whitespace-delimited). Confirmed the
hard way: \code{A2-ai/quarto-plus}'s own \code{Contributes} value ("filters,
shortcodes") contains a space, so that row tokenizes to one more
whitespace-separated field than the 3-column header -- which triggers
\code{read.table()}'s classic "data row has one more field than the header"
rule and silently uses the extension id as a \strong{row name} instead of
the \code{Id} column, shifting \code{Version}/\code{Contributes} left into
\code{Id}/\code{Version} for that row only. Trusting \code{df$Id} alone would miss
exactly the extension this package cares most about checking for. This
function instead searches every cell \emph{and} row name of the returned
data frame, so the column shift doesn't matter.
}

\section{A \verb{quarto list extensions} project-resolution gotcha}{

The \verb{quarto list extensions} CLI command resolves the \emph{entire}
project's markdown (crawling every \code{.qmd}'s \verb{\{\{< include >\}\}}
directives and other resource paths) as a side effect of building its
project file list -- not just its own installed-extensions output.
Confirmed the hard way: a report project missing a file an included
\code{.qmd} points at (e.g. copying \code{report.qmd} out of
\verb{examples/demo-report/} without also copying its \verb{scripts/}
directory) makes this command fail with an error about that missing
file, with no mention of extensions at all. This function catches
that and re-raises with a pointer back to the underlying Quarto error
rather than letting Quarto's raw (and, over \code{quarto_render()}'s R
wrapper, deeply nested) failure propagate uninterpreted.
}

