% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/header-helper.R
\name{.header_helper_ask_scalar}
\alias{.header_helper_ask_scalar}
\title{Prompt for one registry-driven scalar field}
\usage{
.header_helper_ask_scalar(f, fm)
}
\arguments{
\item{f}{One field spec from \code{.quartifyr_header_fields()}.}

\item{fm}{Frontmatter assembled so far -- \code{f$required()}/\code{f$recommended()}
are evaluated against this, not a fresh/empty list, since some fields
depend on an earlier answer (see \code{.header_helper_collect()}'s comment).}
}
\value{
The entered string, or \code{NULL} if skipped/left blank.
}
\description{
Required fields are asked directly; recommended/optional fields are
asked as an include-or-skip question first. An empty answer to a
required field leaves it unset (\code{validate_header()} will flag it, this
function doesn't force an answer).
}
\keyword{internal}
