% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install-extension.R
\name{install_quartifyr_extension}
\alias{install_quartifyr_extension}
\title{Install the quartifyr Quarto extension into a project}
\usage{
install_quartifyr_extension(path = ".", force = FALSE)
}
\arguments{
\item{path}{Target project directory. Defaults to the current directory.}

\item{force}{Overwrite an existing \verb{_extensions/quartifyr/} in \code{path}.
Default \code{FALSE} (errors if one already exists and differs).}
}
\value{
The installed extension path (invisibly).
}
\description{
Copies the bundled \verb{inst/extensions/quartifyr/} into
\verb{<path>/_extensions/quartifyr/}. A plain file copy, not a
\code{quarto::quarto_add_extension()} call: that function only accepts an
archive or a GitHub repository as its \code{extension} argument, not a
bundled local package path, so it isn't the right tool for installing
an extension that ships inside this R package itself. Quarto doesn't
follow symlinks for extensions (confirmed: \verb{quarto render} fails
outright against a symlinked \verb{_extensions/quartifyr/}), so a real
physical copy is required -- the same reason
\code{scripts/sync_demo_extension.py} keeps the two bundled examples'
copies in sync with this package's own canonical one.
}
\details{
Also runs \code{\link[=check_quarto_extensions]{check_quarto_extensions()}} (\code{error = FALSE}) afterward as a
courtesy nudge -- quartifyr's own extension layers on top of
\code{quarto-plus}'s ToC/List of Tables/List of Figures/abbreviations
machinery, which this function doesn't install itself (it isn't
bundled in this R package the way quartifyr's own extension is).
}
\examples{
\dontrun{
# Requires the quarto CLI on PATH (this function's own follow-up
# check_quarto_extensions() call shells out to it).
install_quartifyr_extension("path/to/project")
}
}
