% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate-header.R
\name{.header_structural_checks}
\alias{.header_structural_checks}
\title{Structural front-matter checks beyond the per-field registry}
\usage{
.header_structural_checks()
}
\value{
A list of \code{list(id, severity, check)} specs. \code{severity} is
\code{"error"} (breaks or silently no-ops the render -- see each message)
or \code{"warn"} (works, but likely not what's intended).
}
\description{
Each check is \verb{function(fm, shell_qmd, quarto_yml)} returning \code{NULL}
(no problem) or a single message string. Registered here (rather than
as one-off \code{if} statements inside \code{validate_header()}) for the same
"evergreen" reason the field registry is: adding a new cross-field rule
shouldn't require touching \code{validate_header()}'s own control flow.
}
\keyword{internal}
